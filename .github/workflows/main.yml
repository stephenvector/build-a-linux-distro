name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build docker image
      run: docker image build . -t linux:os
    - name: Build the actual OS
      run: docker run linux:os
